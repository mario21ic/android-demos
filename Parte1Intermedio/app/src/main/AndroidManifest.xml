<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mario21ic.parte1intermedio">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DistanciaDosPuntos"></activity>
        <activity android:name=".MiClusterMap" />
        <activity android:name=".MapaCalor" />
        <activity android:name=".MiPolygon" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAku6RtT8-ZAjFginSfkVW-EJAYR32CgpE" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity android:name=".MiPolygon" />
        <activity android:name=".MiPolyLine" />
        <activity android:name=".MainActivity" />
        <activity android:name=".MisEjemplos">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>